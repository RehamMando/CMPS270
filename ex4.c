#include <stdio.h>


char ** merge(char* a1[], char* a2[], int size1, int size2){
    int n = size1 + size2;
    char **result;
    result = (char**)malloc(sizeof(char*)*n);
    for(int i = 0; i < n; i++){
        
    }
}